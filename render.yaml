services:
  - type: web
    name: your-bot-name
    runtime: python
    buildCommand: pip install --platform manylinux2014_x86_64 --target ./python -r requirements.txt
    startCommand: python bot.py
